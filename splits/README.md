# Split Files

Place one case identifier per line in each file:

- `train_examples.txt`
- `val_examples.txt`
- `test_examples.txt`

The default configs point to these relative paths. Do not commit private patient
identifiers or institution-specific paths when preparing a public release.
